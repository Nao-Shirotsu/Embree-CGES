name: build

on: 
  push:
    branches:
      - autobuild_setup

  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps  :
      - name : install
        run  : ./instal.sh
        shell: bash